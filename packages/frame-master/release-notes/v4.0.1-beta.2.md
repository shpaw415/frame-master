## 4.0.1-beta.2

`createPluginTestEnv().build()` now runs every `BuildUnifier` pipeline, matching `frame-master build`.

- Sets `BUILD_MODE` for the call, then restores the previous value.
- Returns the default builder output. A failed pipeline still throws.
