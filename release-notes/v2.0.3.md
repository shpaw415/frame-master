# Release Notes - Frame Master v2.0.3

**Release Date:** November 5, 2025

## 🎯 Overview

This patch release addresses critical CLI stability issues and enhances the build pipeline with improved error handling and automated cleanup capabilities. This update ensures better reliability for developers using Frame Master's command-line interface.

## 🐛 Bug Fixes

### CLI Stability

- **Fixed TypeScript configuration parsing**: Resolved an issue where the `create` and `init` commands would fail when encountering TypeScript configuration files (`tsconfig.json`) containing comments. The parser now properly handles commented configuration files, ensuring seamless project initialization.

## ✨ Improvements

### Enhanced Build Pipeline

- **Improved error handling**: The `build` command now provides more comprehensive error reporting with clearer diagnostic messages and enhanced output formatting for better developer experience.
- **Automated cleanup process**: Implemented automatic removal of leftover files in the build directory after successful builds, ensuring a cleaner workspace and preventing potential conflicts in subsequent builds.

## 🔧 Technical Details

`v2.0.2` introduced a change in the order of initialization of the builder and the configs.

This release focuses on robustness and developer experience improvements without introducing breaking changes. All existing functionality remains compatible with previous versions.
